
  # iOS Toggle Switch Animation (Copy)

  This is a code bundle for iOS Toggle Switch Animation (Copy). The original project is available at https://www.figma.com/design/W61ld4TDxYjRPzFwpAEbRX/iOS-Toggle-Switch-Animation--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  